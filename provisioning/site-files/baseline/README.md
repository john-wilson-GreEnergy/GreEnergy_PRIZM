# Baseline
Required for baseline:
- deploy-redux.tar

This file should be copied here by the technician.
It should contain `deploy/featherScript.sh`.
PRIZM validates the tar listing without extracting it when possible.
